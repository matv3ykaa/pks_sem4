<Window x:Class="LibraryApp.Views.AuthorsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:LibraryApp.ViewModels"
        xmlns:conv="clr-namespace:LibraryApp.Converters"
        Title="Управление авторами" Height="550" Width="820"
        WindowStartupLocation="CenterOwner"
        Background="#F8FAFC">
    <Window.DataContext>
        <vm:AuthorsViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <conv:InverseBoolToVisibilityConverter x:Key="InvBoolToVis"/>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="320"/>
        </Grid.ColumnDefinitions>

        <!-- Left: List -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Авторы" FontSize="16" FontWeight="Bold"
                       Foreground="#1E293B" Margin="0,0,0,10"/>

            <DataGrid Grid.Row="1" ItemsSource="{Binding Authors}"
                      SelectedItem="{Binding SelectedAuthor}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="*"/>
                    <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="*"/>
                    <DataGridTextColumn Header="Страна" Binding="{Binding Country}" Width="100"/>
                    <DataGridTextColumn Header="Дата рождения" Binding="{Binding BirthDate}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
                <Button Content="+ Добавить" Margin="0,0,8,0" Style="{StaticResource PrimaryButton}"
                        Command="{Binding StartAddCommand}"/>
                <Button Content="Изменить" Margin="0,0,8,0" Style="{StaticResource SuccessButton}"
                        Command="{Binding StartEditCommand}"/>
                <Button Content="Удалить" Style="{StaticResource DangerButton}"
                        Command="{Binding DeleteCommand}"/>
            </StackPanel>
        </Grid>

        <!-- Right: Form (visible when editing) -->
        <Border Grid.Column="2" Background="White" CornerRadius="8"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                Padding="20"
                Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="14"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="14"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="14"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="14"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="{Binding FormTitle}" FontSize="15" FontWeight="Bold"
                           Foreground="#1E293B"/>

                <StackPanel Grid.Row="2">
                    <Label Content="Фамилия *"/>
                    <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Row="4">
                    <Label Content="Имя *"/>
                    <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Row="6">
                    <Label Content="Страна"/>
                    <TextBox Text="{Binding Country, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Row="8">
                    <Label Content="Дата рождения"/>
                    <DatePicker SelectedDate="{Binding BirthDate}" FontSize="13"/>
                </StackPanel>

                <StackPanel Grid.Row="10" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Отмена" Margin="0,0,8,0" Style="{StaticResource SecondaryButton}"
                            Command="{Binding CancelCommand}"/>
                    <Button Content="Сохранить" Style="{StaticResource PrimaryButton}"
                            Command="{Binding SaveCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Placeholder when not editing -->
        <Border Grid.Column="2" Background="White" CornerRadius="8"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                Padding="20"
                Visibility="{Binding IsEditing, Converter={StaticResource InvBoolToVis}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Выберите автора&#10;или нажмите «+ Добавить»"
                           TextAlignment="Center" Foreground="#9CA3AF" FontSize="13" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
